const {chromium}=require(process.env.SPHERE_PLAYWRIGHT||'playwright'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:process.env.SPHERE_BROWSER});try{const page=await browser.newPage({viewport:{width:1600,height:1000}});await page.goto((process.env.SPHERE_URL||'http://127.0.0.1:8766/')+'work/screenshots/wound-materials/review.html');const result=await page.evaluate(async()=>{await Promise.all([...document.images].map(img=>{img.loading='eager';return img.decode();}));return {images:document.images.length,failed:[...document.images].filter(i=>!i.naturalWidth).length,overflow:document.documentElement.scrollWidth>innerWidth};});assert.equal(result.failed,0);assert.equal(result.overflow,false);await page.screenshot({path:path.resolve('work/screenshots/wound-materials/gallery.png')});console.log(result);}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
